A simple python script to convert an .xls or xlsx file to .csv file:

Open Command Prompt and install pandas: pip install pandas

Open Command Prompt and install the openpyxl library: pip install openpyxl

